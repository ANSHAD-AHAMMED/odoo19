<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Action -->
    <record id="action_property_tags" model="ir.actions.act_window">
        <field name="name">Property tags</field>
        <field name="res_model">property_tags</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">add your property here</p>
        </field>
    </record>

    <!-- List View -->
    <record id="view_property_tags_list" model="ir.ui.view">
        <field name="name">property_tags.list</field>
        <field name="model">property_tags</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_property_tags_form" model="ir.ui.view">
        <field name="name">property_tags.form</field>
        <field name="model">property_tags</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color" widget="color_picker"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>


