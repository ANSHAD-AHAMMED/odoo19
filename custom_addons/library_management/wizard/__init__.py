# -*- coding: utf-8 -*-
from . import suggestion_wizard
from . import library_report_wizard
"""
currently if i filter by partner(member), it will filter by the report only based on that partner. but in the output every line contains that person name in the name column. i dont want that. if i select a member for filtering process that person name need to display top of the report and other details are on the normal way like before but not include the column for member, member name only show top of the report  
"""